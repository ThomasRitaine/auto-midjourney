<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= clientName %> Collection</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= clientName %> Collection</h1>
    <div id="imageContainer">
        <% images.forEach(image => { %>
            <img src="/image/<%= clientName %>/<%= image %>" alt="<%= image %>" width="33%">
        <% }); %>
    </div>
    <!-- <div id="imageContainer">
        <% images.forEach(image => { %>
            <img src="/images/<%= image %>" alt="<%= image %>" width="33%">
        <% }); %>
    </div> -->

</body>
</html>
